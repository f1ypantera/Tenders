@model IEnumerable<Tenders.Models.ViewTender>



</br>
<div class="form-group">

    <a class="btn btn-default" href=" @Url.Action("Create", "ViewTenders")">Создать новый вид</a>

</div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ListViewTenders)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ListViewTenders)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.ViewTenderID }) |    
            @Html.ActionLink("Удалить", "Delete", new { id=item.ViewTenderID })
        </td>
    </tr>
}

</table>
