@model IEnumerable<Tenders.Models.CurrencyBudget>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ListCurrencyBudget)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ListCurrencyBudget)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CurrencyBudgetID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CurrencyBudgetID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CurrencyBudgetID })
        </td>
    </tr>
}

</table>
